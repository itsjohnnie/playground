{
  "name": "poetry-camera-previewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local previewer for poetry.camera — upload an image, edit per-mode prompts, print a poem.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch-path server.js --watch-path .env server.js",
    "sync": "node sync.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
